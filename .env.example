# Copy to .env.local and fill in. Documented in .agents/docs/INSTALLATION.md.

# SQLite file path.
DATABASE_URL=file:./dev.db

# Provider is not chosen yet, see AGENTS.md Open Items.
# Whatever is picked is read only by /lib/ai, nothing else imports a provider SDK.
AI_PROVIDER=
AI_PROVIDER_API_KEY=
